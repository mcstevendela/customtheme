{#
  Title: Banner
  Description: Banner block
  Category: RD-blocks
  Icon: money
  Keywords: banner
  Mode: edit
  Align: left
  PostTypes: page post
  SupportsAlign: left right
  SupportsMode: true
  SupportsMultiple: true
#}

<section class="rd-block rd-spacing rd-spacing--white rd-banner rd-banner-home">
  <div class="rd-banner-bg">
    {% if fields.background %}
      <img src="{{ fields.background.url }}" alt="{{ fields.background.alt }}" class="rd-banner-bg__image rd-responsive hidden lg:block">
      <img src="{{ fields.background_mobile.url }}" alt="{{ fields.background_mobile.alt }}" class="rd-banner-bg__image rd-responsive lg:hidden">
    {% endif %}
  </div>
  <div class="rd-container rd-banner-wrapper">
    <div class="rd-banner-content">
      {% if fields.subtitle %}
        <span class="rd-banner__tag">{{ fields.subtitle }}</span>
      {% endif %}
      {% if fields.title %}
        <h1 class="rd-banner__title">{{ fields.title }}</h1>
      {% endif %}
      {% if fields.description %}
        <p class="rd-banner__text">{{ fields.description }}</p>
      {% endif %}
      {% if fields.button %}
        <a href="{{ fields.button.url }}" target="{{ fields.button.target }}" class="rd-banner__button btn btn-primary">{{ fields.button.title }}</a>
      {% endif %}
    </div>
    {% if fields.form %}
    <div class="rd-form rd-banner-form">
      <span class="rd-banner-form__title">{{ fields.form_title }}</span>
      {{ fields.rating|raw }}
      {{ fields.form|raw }}
    </div>
    {% endif %}
  </div>
</section>